# تهيئة نظام التقارير

## ما هو جاهز

يحتوي النظام على تقرير إداري موحّد في المسار `/admin/reports`. تعتمد الصفحة على endpoint محمي للمدير هو `GET /api/reports/overview?days=30`، ويقبل الفترات 7 أو 30 أو 90 أو 365 يومًا. يعرض التقرير مؤشرات الحجوزات والمدفوعات والمستخدمين والسيارات والشكاوى والتقييمات، إضافة إلى أفضل السيارات والموردين، مع تصدير CSV من الواجهة.

## تهيئة قاعدة البيانات

يجب تشغيل migrations قبل تشغيل الخادم. يقرأ المشغّل الملفات الرقمية بالترتيب ويسجل الملفات المنفذة في جدول `schema_migrations`:

```bash
cd backend
npm install
npm run migrate
```

أضيفت migration رقم `049_reporting_indexes.sql` لإنشاء فهارس على تواريخ وحالات جداول المستخدمين والسيارات والحجوزات والمدفوعات والتقييمات والشكاوى. الفهارس تستخدم `IF NOT EXISTS` ويمكن تشغيلها بأمان مرة واحدة عبر المشغّل المعتاد.

## تشغيل النظام

```bash
cd backend
npm run dev
```

وفي طرفية أخرى:

```bash
cd frontend
npm install
npm run dev
```

بعد تسجيل الدخول بحساب إداري، افتح:

```text
/admin/reports
```

## فحص التقارير

```bash
cd backend
npm run check:reports
```

ويُستحسن أيضًا تنفيذ فحص الواجهة:

```bash
cd frontend
npm run lint
npm run build
```

## متطلبات البيانات

تعتمد التقارير على الجداول `users`, `cars`, `reservations`, `payments`, `reviews`, و`complaints`. إذا كانت بعض الجداول فارغة فستظهر الأرقام بصفر، وهذا سلوك صحيح وليس خطأ اتصال.

## الصلاحيات

الوصول إلى `/api/reports/overview` مقيد بالـmiddleware `protect` و`authorize('admin')`. لا ينبغي إزالة هذا القيد أو عرض بيانات التقارير العامة بدون إضافة سياسة صلاحيات منفصلة.
