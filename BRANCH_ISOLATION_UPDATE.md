# ملخص تعديل عزل الفروع

## النتيجة
تم تنفيذ الجزء الأساسي من عزل الفروع دون تغيير صلاحيات حساب المورد الرئيسي. أصبح بالإمكان إنشاء حساب دخول مستقل للفرع، وإصدار توكن يحتوي على نطاق المورد والفرع، ثم فرض هذا النطاق على الخادم بدل الاعتماد على `location_id` القادم من الواجهة.

عند إنشاء الفرع أصبح النظام يطلب **اسم مدير الفرع وبريده وكلمة مروره**. يتحقق الخادم من أن البريد غير مستخدم في المستخدمين أو الموظفين أو حسابات الفروع، ثم ينشئ حساب المدير داخل نفس المعاملة مع إنشاء الفرع. لذلك لا يمكن أن ينجح إنشاء فرع جديد دون مدير مرتبط به.

## التعديلات

1. أضيف ترحيل قاعدة البيانات `055_branch_accounts.sql` لإنشاء جدول حسابات الفروع مع ربط إلزامي بالمورد والفرع، ومنع تكرار الحساب للفرع أو البريد، والتحقق من ملكية المورد للفرع.
2. أضيفت مسارات إنشاء حساب الفرع وتسجيل الدخول تحت `/api/auth/branch`:
   - `POST /api/auth/branch/account` لإنشاء الحساب من المورد المالك.
   - `POST /api/auth/branch/login` لتسجيل الدخول وإصدار توكن بنطاق `supplier_id` و`branch_id`.
   - إنشاء الفرع من شاشة المورد ينشئ حساب مدير الفرع تلقائيًا، بينما لا يسمح لمدير فرع بإنشاء فروع إضافية.
3. تم تحديث middleware المصادقة ليعيد تحميل حساب الفرع من قاعدة البيانات في كل طلب، ويتحقق من حالة الحساب والفرع والاشتراك قبل السماح بالوصول.
4. تم تعديل قوائم سيارات المورد وحجوزاته بحيث يفرض حساب الفرع فرعه من التوكن ويتجاهل أي `location_id` مخالف يرسله العميل.
5. تم تصحيح عمليات اعتماد ورفض وإتمام الحجوزات لاستخدام هوية المورد المالك عند العمل من حساب فرع.
6. أضيف المسار `GET /api/reports/branch/overview` لإرجاع مؤشرات وتقسيمات حجوزات مقيدة بفرع التوكن على مستوى SQL.
7. أضيف الترحيل `057_reservation_lifecycle_constraints.sql` لإزالة قيود الحالة القديمة المتعارضة، وتوحيد قبول `pending_payment` و`disputed` في `status` و`handover_state`.
8. أصبح الحجز الجديد يبدأ بحالة `pending_payment`، وبعد الدفع ينتقل إلى `pending` ليظهر للمورد للمراجعة. كما تم تحديث تسميات الواجهة وإتاحة الإلغاء قبل الدفع.
9. أضيف الترحيل `056_branch_account_email_guard.sql` لتوحيد منع تكرار البريد بين المستخدمين والموظفين ومديري الفروع، مع تجاهل المسافات واختلاف حالة الأحرف.

## التحقق

- اجتاز كل ملف JavaScript معدل فحص `node --check`.
- اجتاز الأمر `npm run check:reports`.
- نجح بناء الواجهة الأمامية باستخدام `npm run build` بعد إضافة حقول مدير الفرع إلى نموذج إنشاء الفرع.
- اجتاز التغيير `git diff --check`.
- لم يُشغّل ترحيل قاعدة البيانات فعليًا لأن بيئة التنفيذ الحالية لا تحتوي على إعداد اتصال قاعدة بيانات. يجب تشغيل الترحيل في بيئة المشروع المتصلة بقاعدة البيانات قبل استخدام تسجيل دخول الفروع.

## ملاحظة نطاق
هذا التعديل يغطي طبقة الهوية والنطاق والمسارات الأساسية المذكورة أعلاه. ما زالت العمليات المالية والتسليم والشكاوى والتقييمات تحتاج تحديثات مماثلة إذا كان المطلوب عزلًا كاملًا لكل نطاق في جميع عمليات النظام.
