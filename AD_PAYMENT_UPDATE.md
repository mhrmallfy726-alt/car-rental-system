# تحديث الدفع وجدولة الإعلانات

1. شغّل ترحيل قاعدة البيانات: `node backend/run_migration.js` من مجلد المشروع، أو نفّذ الملف `backend/migrations/029_advertisement_daily_schedule.sql` على قاعدة البيانات.
2. من مركز الإدارة المالية اضبط سعر الإعلان لليوم ووقت البداية والنهاية اليومية.
3. يقرأ المورد الإعدادات تلقائيًا عند فتح صفحة طلب الإعلان، ويظهر له السعر اليومي والإجمالي ووقت الظهور.
4. بعد اعتماد الطلب، لا يتم تفعيل الإعلان ولا تسجيل الدفع حتى يضغط المورد زر الدفع.
5. مسار الدفع الجديد هو `POST /api/payments/advertisement-checkout`، وبعد نجاحه تتحول حالة الإعلان إلى `active` وحالة الدفع إلى `paid`.

ملاحظة: المشروع الحالي يستخدم وضع الدفع المحاكى نفسه المستخدم في الحجوزات؛ استبدال المحاكاة ببوابة حقيقية يحتاج إضافة مزود الدفع وWebhook، مع إبقاء نفس دورة الحالات.
