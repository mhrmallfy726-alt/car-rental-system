# ملف تسليم وتصحيح Manus — صفحة السيارات وإدارة الموافقات

**المستودع:** `https://github.com/mhrmallfy726-alt/car-rental-system.git`  
**الفرع:** `main`  
**تاريخ التسليم:** 2026-09-11  

## الهدف

تم إصلاح وتطوير صفحات السيارات الخاصة بالمورد والأدمن، مع تحسين تجربة الهاتف، وتصحيح اختيار المركز الرئيسي، وإضافة مسار مراجعة وموافقة السيارة، ومعالجة أخطاء تحميل الصور وفشل API. هذا الملف مخصص للحساب الآخر كي يفهم ما تم تنفيذه ويكمل العمل دون إعادة التعديلات أو كسرها.

## آخر Commit

آخر تعديل مرفوع على GitHub هو:

```text
b411aad — Make admin car details resilient to API failures
```

والـ commits المرتبطة بتسلسل العمل هي:

| Commit | التعديل |
|---|---|
| `1bc4e3b` | جعل المركز الرئيسي الافتراضي عند إضافة السيارة وإزالة الخيار الوهمي للفرع |
| `6ed8e4e` | إضافة بطاقات سيارات الهاتف وجعل سنة الصنع قابلة للتعديل |
| `2f8b741` | إضافة صفحة تفاصيل ومراجعة السيارة للأدمن وتصحيح مسار إشعار الموافقة |
| `a7907e4` | تحسين قائمة الأدمن الهاتفية وإصلاح روابط صور السيارات |
| `d059f53` | منع اختفاء صفحة تفاصيل السيارة عند فشل API وإضافة fallback |
| `6fb9bf1` | إعادة تصميم قائمة سيارات الأدمن كبطاقات هاتف ومعالجة فشل API بزر إعادة المحاولة |
| `b411aad` | تمرير بيانات السيارة من القائمة إلى صفحة التفاصيل لمنع فشل زر التفاصيل |

## الملفات التي تم تعديلها

### 1. صفحة سيارات المورد

```text
frontend/src/pages/supplier/AddCar.jsx
frontend/src/pages/supplier/MyCars.jsx
```

تم تنفيذ الآتي:

- المركز الرئيسي يتم اختياره تلقائيًا عند إضافة السيارة.
- إزالة خيار placeholder الذي كان يظهر كأنه فرع إضافي ويطلب اختيارًا يدويًا.
- إخفاء UUID والمعرّفات التقنية من أسماء الفروع.
- إرسال `location_id` داخليًا فقط عند الحفظ.
- إذا لم يحدد المستخدم فرعًا، تستخدم الخلفية أول مركز رئيسي نشط تلقائيًا.
- سنة الصنع أصبحت قابلة للمسح والكتابة والتعديل.
- التحقق يسمح بالقيم من 1980 حتى العام القادم.
- صفحة `/supplier/cars` تعرض على الهاتف بطاقات تحتوي على:
  - صورة السيارة.
  - الشركة والموديل.
  - رقم اللوحة.
  - سنة الصنع.
  - الفئة.
  - السعر اليومي.
  - الحالة.
  - الخصم.
  - أزرار العروض والتعديل والحذف.
- عرض الجدول محفوظ للشاشات الكبيرة.

### 2. صفحة سيارات الأدمن

```text
frontend/src/pages/admin/AdminCars.jsx
```

مسار الصفحة:

```text
/admin/cars
```

تم تنفيذ الآتي:

- استخدام مكوّن `AdminSidebar` الموحد بدل القائمة القديمة.
- في الهاتف تظهر قائمة الأدمن كزر `القائمة` أعلى الشاشة.
- القائمة تفتح من اليمين وتحتوي على خلفية وزر إغلاق.
- على الهاتف تظهر السيارات كبطاقات، وليس جدولًا أفقيًا.
- كل بطاقة تحتوي على:
  - صورة السيارة.
  - اسم السيارة والموديل.
  - سنة الصنع.
  - اسم المورد.
  - الفئة.
  - السعر.
  - رقم اللوحة.
  - حالة الموافقة.
  - زر التفاصيل.
  - زر الموافقة.
- على الكمبيوتر يبقى الجدول ظاهرًا.
- في حالة فشل API لا تظهر صفحة فارغة، بل تظهر رسالة خطأ وزر `إعادة المحاولة`.
- بعد الموافقة يتم تحديث السيارة محليًا إلى `is_approved: true` و`status: available`.

### 3. صفحة تفاصيل ومراجعة السيارة للأدمن

```text
frontend/src/pages/admin/AdminCarDetail.jsx
```

المسار:

```text
/admin/cars/:id
```

تحتوي الصفحة على:

- قائمة الأدمن الجانبية المتجاوبة.
- عنوان السيارة وحالة الموافقة.
- صور السيارة.
- بيانات السيارة.
- سنة الصنع.
- الفئة.
- رقم اللوحة.
- السعر اليومي.
- بيانات المورد.
- بيانات المركز أو الفرع.
- زر `الموافقة على السيارة`.
- تصميم بطاقات مناسب للهاتف.

### 4. قائمة الأدمن الموحدة

```text
frontend/src/components/AdminSidebar.jsx
```

تم تطويرها لتعمل مثل قائمة المورد في الهاتف:

- زر قائمة ثابت أعلى الهاتف.
- لوحة جانبية قابلة للفتح والإغلاق.
- زر إغلاق.
- خلفية overlay.
- منع تمرير الصفحة خلف القائمة.
- إغلاق القائمة تلقائيًا عند الانتقال لمسار جديد.

### 5. إشعار السيارة

```text
frontend/src/pages/NotificationDetail.jsx
backend/src/routes/carRoutes.js
```

تم تصحيح إشعار:

```text
سيارة جديدة بانتظار الموافقة
```

ليفتح للأدمن:

```text
/admin/cars/{carId}
```

كما تم التعامل مع الإشعارات القديمة التي تحتوي رابطًا خاطئًا للحجوزات، بحيث يتم تجاهل الرابط القديم إذا كان نوع الإشعار `car`، ويفتح مسار مراجعة السيارة الصحيح.

### 6. API تفاصيل السيارة

```text
backend/src/routes/adminRoutes.js
frontend/src/services/api.js
```

تمت إضافة:

```text
GET /api/admin/cars/:id
```

ويعيد:

- بيانات السيارة.
- اسم المورد وبريده وهاتفه.
- اسم الفئة.
- اسم المركز والمدينة والعنوان.
- صور السيارة.

وفي الواجهة:

```javascript
adminAPI.getCar(id)
```

## معالجة الصور

### المشكلة السابقة

صور السيارات كانت تُخزن أحيانًا بمسار مطلق داخلي مثل مسار ملفات الخادم، أو كانت الواجهة تحاول تحميلها من `localhost` عند النشر.

### الإصلاح

تم تعديل:

```text
backend/src/routes/carRoutes.js
frontend/src/utils/imageUtils.js
```

ليتم حفظ الصور الجديدة بالشكل العام:

```text
/uploads/file-name.jpg
```

وتم جعل `getImageUrl`:

- يستخدم عنوان الموقع الحالي إذا لم يوجد `VITE_API_URL`.
- يدعم الصور القديمة التي تحتوي على مسار داخلي ويستخرج منها جزء `uploads/`.
- يحافظ على الروابط الخارجية التي تبدأ بـ `http`.

## سلوك زر التفاصيل

تم حل مشكلة زر التفاصيل الذي كان يظهر خطأ API.

عند الضغط على التفاصيل من قائمة الأدمن، يتم الآن تمرير بيانات السيارة مباشرة:

```javascript
navigate(`/admin/cars/${car.id}`, { state: { car } })
```

وفي صفحة التفاصيل يتم استخدام البيانات المرسلة فورًا، ثم محاولة تحميل التفاصيل الإضافية في الخلفية. لذلك لا تختفي الصفحة إذا فشل API التفاصيل.

روابط الجدول تستخدم أيضًا:

```jsx
<Link to={`/admin/cars/${car.id}`} state={{ car }}>
```

## اختبارات تم تنفيذها

تم تنفيذ الاختبارات التالية بنجاح:

```bash
cd frontend
npm run build
```

و:

```bash
cd ..
node --test frontend/test-input-validation.mjs
```

وفحص صياغة Backend:

```bash
node --check backend/src/routes/adminRoutes.js
node --check backend/src/routes/carRoutes.js
```

كما تم تشغيل:

```bash
git diff --check
```

## المطلوب من الحساب الآخر قبل متابعة أي تعديل

1. سحب آخر نسخة:

```bash
git pull --ff-only origin main
```

2. التأكد أن آخر commit هو:

```text
b411aad
```

3. اختبار الصفحة بحساب أدمن حقيقي على:

```text
/admin/cars
```

4. الضغط على زر **التفاصيل** من بطاقة الهاتف ومن الجدول.

5. التحقق من أن المسار يصبح:

```text
/admin/cars/{id}
```

وليس مسار الحجوزات.

6. التحقق من ظهور قائمة الأدمن في الهاتف عبر زر `القائمة`.

7. التحقق من ظهور صورة السيارة.

8. إذا ظهر خطأ API، يجب فحص استجابة:

```text
GET /api/admin/cars
GET /api/admin/cars/{id}
```

مع فحص سجل Backend وقاعدة البيانات.

9. التأكد من أن ترحيلات قاعدة البيانات، خصوصًا ترحيل `031_supplier_showroom_name.sql`، مطبقة في البيئة المنشورة حتى يكون العمود التالي موجودًا:

```text
locations.showroom_name
```

10. التأكد من وجود العمود التالي في جدول الإشعارات قبل اعتماد إشعارات السيارات الجديدة:

```text
notifications.action_url
```

## ملاحظة مهمة عن النشر

الكود مرفوع على فرع `main`، لكن يجب التأكد من أن Vercel أو خدمة النشر مرتبطة بهذا الفرع وتنفذ إعادة نشر فعلية بعد كل push. إذا بقي الموقع يعرض الواجهة القديمة، فالمشكلة غالبًا من:

- عدم تنفيذ Deployment جديد.
- استخدام مشروع Vercel مختلف عن هذا المستودع.
- Cache قديم.
- متغير `VITE_API_URL` غير مضبوط.
- Backend المنشور ليس نفس النسخة الموجودة في هذا المستودع.

## تعليمات عدم كسر العمل

- لا تحذف `AdminSidebar` ولا تستبدله بقائمة ثابتة داخل صفحة جديدة.
- أي صفحة أدمن جديدة يجب أن تستخدم `AdminSidebar`.
- أي صفحة مورد جديدة يجب أن تستخدم `SupplierSidebar`.
- لا تجعل زر التفاصيل يعتمد على API منفصل قبل عرض البيانات المتوفرة من القائمة.
- لا تعرض UUID أو المسارات الداخلية للصور للمستخدم.
- عند إضافة endpoint جديد، يجب اختبار الاستجابة من Backend مع حساب أدمن فعلي.
- لا تعدّل مسار `/admin/cars` إلى مسار الحجوزات.
- يجب أن تبقى صلاحيات الموافقة محمية بـ `protect` و`authorize('admin')` في Backend.

## الخلاصة

الحالة الحالية تشمل تصميم الهاتف، البطاقات، القائمة الجانبية، إشعار الموافقة، صفحة التفاصيل، زر الموافقة، معالجة فشل API، وتحسين روابط الصور. آخر نقطة يجب التحقق منها هي النشر الفعلي واتصال Frontend بالـ Backend وقاعدة البيانات في البيئة الإنتاجية.
