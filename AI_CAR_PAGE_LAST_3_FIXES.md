# تعليمات متابعة للذكاء الاصطناعي — آخر 3 تعديلات في صفحة سيارات الأدمن

## نطاق العمل

المطلوب متابعة وفحص **آخر ثلاثة تعديلات فقط** المتعلقة بصفحة سيارات الأدمن. لا تعِد تنفيذ أعمال المورد أو الإعلانات أو الموظفين، ولا تغيّر التصميم العام خارج النطاق إلا إذا كان ضروريًا لإصلاح هذه الصفحة.

المستودع:

```text
https://github.com/mhrmallfy726-alt/car-rental-system.git
```

الفرع:

```text
main
```

ابدأ دائمًا بـ:

```bash
git pull --ff-only origin main
```

---

## التعديل الأول: قائمة الأدمن الجانبية في الهاتف

### الملفات

```text
frontend/src/components/AdminSidebar.jsx
frontend/src/pages/admin/AdminCars.jsx
frontend/src/pages/admin/AdminCarDetail.jsx
```

### المطلوب الحفاظ عليه

صفحات الأدمن يجب أن تستخدم مكوّن:

```jsx
<AdminSidebar />
```

ولا تُنشئ قائمة جانبية جديدة داخل صفحة السيارات.

في الهاتف يجب أن يظهر:

- زر `القائمة` أعلى الشاشة.
- فتح القائمة من اليمين.
- زر إغلاق.
- خلفية overlay.
- إغلاق القائمة عند تغيير المسار.
- منع تمرير الصفحة خلف القائمة.

قواعد CSS الحالية تعتمد على:

```css
.admin-mobile-header
.admin-mobile-menu-button
.admin-sidebar.is-mobile-open
.admin-sidebar-overlay
body.admin-menu-open
```

عند إضافة صفحة أدمن جديدة، استخدم `AdminSidebar` بدل القائمة القديمة الثابتة.

---

## التعديل الثاني: بطاقات صفحة `/admin/cars` ومعالجة فشل قائمة السيارات

### الملف

```text
frontend/src/pages/admin/AdminCars.jsx
```

### المسار

```text
/admin/cars
```

### السلوك المطلوب

على الشاشات الكبيرة:

- عرض جدول السيارات.

على الهاتف:

- إخفاء الجدول.
- عرض السيارات داخل بطاقات `.admin-mobile-car-card`.
- البطاقة تعرض:
  - صورة السيارة.
  - الشركة والموديل.
  - سنة الصنع.
  - اسم المورد.
  - الفئة.
  - السعر اليومي.
  - رقم اللوحة.
  - الحالة.
  - زر التفاصيل.
  - زر الموافقة.

المكوّنات المساعدة الموجودة في الملف:

```text
CarCard
CarImage
Status
ApproveButton
```

### API القائمة

يتم جلب القائمة عبر:

```javascript
adminAPI.getCars()
```

وهو:

```http
GET /api/admin/cars
```

عند فشل الطلب:

- لا تعرض صفحة فارغة.
- اعرض رسالة `تعذر تحميل السيارات`.
- اعرض زر `إعادة المحاولة`.
- زر إعادة المحاولة يعيد استدعاء `fetchCars`.

لا تحذف حالة `loading` أو `errorMessage` أو `fetchCars`.

---

## التعديل الثالث: زر التفاصيل ومنع اعتماد الصفحة على API منفصل

### الملفات

```text
frontend/src/pages/admin/AdminCars.jsx
frontend/src/pages/admin/AdminCarDetail.jsx
```

### المشكلة التي تم إصلاحها

كان الضغط على زر تفاصيل السيارة يستدعي API التفاصيل فقط، وعند فشله تختفي الصفحة أو تظهر رسالة خطأ.

### السلوك الحالي المطلوب

من بطاقة الهاتف:

```javascript
navigate(`/admin/cars/${car.id}`, { state: { car } })
```

ومن جدول الكمبيوتر:

```jsx
<Link to={`/admin/cars/${car.id}`} state={{ car }}>
```

صفحة التفاصيل تقرأ البيانات الأولية من:

```javascript
location.state?.car
```

وتستخدمها فورًا قبل انتظار API التفاصيل.

### مسار التفاصيل

```text
/admin/cars/:id
```

### API التفاصيل

```http
GET /api/admin/cars/:id
```

ويتم استدعاؤه عبر:

```javascript
adminAPI.getCar(id)
```

إذا نجح الطلب، حدّث البيانات بالتفاصيل الموسعة.

إذا فشل الطلب وكانت بيانات السيارة موجودة في `location.state`:

- لا تعمل redirect.
- لا تجعل `car` تساوي `null`.
- أبقِ البطاقة ظاهرة.
- اعرض رسالة غير معيقة تفيد بأن التفاصيل الإضافية غير متاحة.

إذا فشل الطلب ولا توجد بيانات في `location.state`:

- استخدم fallback عبر:

```javascript
adminAPI.getCars()
```

- ابحث عن السيارة حسب `id`.
- اعرض بيانات القائمة الأساسية.
- لا تعرض صفحة بيضاء.
- عند عدم العثور على السيارة، اعرض زر العودة إلى `/admin/cars`.

لا تحذف `AdminSidebar` من صفحة التفاصيل.

---

## صلاحية الموافقة

زر الموافقة يستدعي:

```javascript
adminAPI.approveCar(id)
```

والمسار:

```http
PUT /api/admin/cars/:id/approve
```

بعد النجاح حدّث الحالة محليًا:

```javascript
is_approved: true
status: 'available'
```

لا تضع صلاحية الموافقة في الواجهة فقط؛ يجب أن تبقى حماية Backend موجودة عبر صلاحية الأدمن.

---

## فحص API Backend قبل أي تعديل جديد

الملف:

```text
backend/src/routes/adminRoutes.js
```

يجب التأكد من وجود:

```text
GET /cars
GET /cars/:id
PUT /cars/:id/approve
```

ويجب ألا يكون ترتيب المسارات سببًا في اعتراض `/cars/:id` بواسطة مسار آخر.

استعلام التفاصيل يعرض:

- بيانات السيارة.
- المورد.
- الفئة.
- المركز أو الفرع.
- الصور.

إذا فشل استعلام التفاصيل بسبب عمود قاعدة بيانات مفقود، أصلح Migration أو الاستعلام، ولا تحذف fallback الموجود في الواجهة.

---

## الاختبارات الإلزامية بعد أي تعديل

```bash
cd frontend
npm run build
```

```bash
cd ..
node --test frontend/test-input-validation.mjs
```

```bash
node --check backend/src/routes/adminRoutes.js
node --check backend/src/routes/carRoutes.js
```

```bash
git diff --check
```

اختبار يدوي مطلوب:

1. افتح `/admin/cars` بحساب أدمن.
2. اختبر الهاتف بعرض أقل من 900px.
3. افتح زر القائمة الجانبية.
4. افتح تفاصيل السيارة من البطاقة.
5. افتح التفاصيل من جدول الكمبيوتر.
6. عطّل API التفاصيل مؤقتًا وتأكد أن بيانات السيارة الأساسية ما زالت تظهر.
7. اختبر زر الموافقة.
8. تأكد أن الصفحة لا تنتقل إلى الحجوزات.

## ممنوعات

- لا تعِد القائمة الجانبية القديمة.
- لا تجعل صفحة التفاصيل تعتمد على `GET /api/admin/cars/:id` وحده.
- لا تحذف تمرير `state={{ car }}`.
- لا تستخدم redirect عند فشل API إذا كانت بيانات السيارة متوفرة.
- لا تعرض UUID أو أخطاء SQL للمستخدم.
- لا تغيّر صفحات المورد ضمن هذا النطاق.
- لا تغيّر مسارات الحجوزات.

## نتيجة متوقعة

صفحة `/admin/cars` تعمل على الكمبيوتر والهاتف، وتعرض القائمة الجانبية كزر في الهاتف، والسيارات كبطاقات، وزر التفاصيل يفتح صفحة واضحة حتى عند فشل API التفاصيل، مع بقاء زر الموافقة يعمل للأدمن.
