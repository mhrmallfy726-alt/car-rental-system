# دليل شامل للمبتدئين: تشغيل نظام "لبيتكم" لتأجير السيارات (Labeitcom)

مرحباً بك! هذا الدليل مصمم خصيصاً لمساعدتك على تشغيل المشروع من الصفر، حتى لو كانت هذه هي المرة الأولى التي تتعامل فيها مع مشاريع البرمجة.

---

## 1. تحميل المشروع
هناك طريقتان للحصول على الكود:
1.  **عبر GitHub (موصى به)**:
    *   افتح رابط المستودع على GitHub.
    *   اضغط على الزر الأخضر **"Code"**.
    *   اختر **"Download ZIP"**.
    *   بعد التحميل، قم بفك الضغط في مكان سهل الوصول إليه (مثلاً: سطح المكتب).
2.  **عبر Git (للمحترفين)**:
    *   افتح Terminal واكتب: `git clone [URL_HERE]`

---

## 2. تثبيت المتطلبات الأساسية (البرامج المطلوبة)

يجب تثبيت البرامج التالية قبل البدء:

### أ- تثبيت Node.js (بيئة التشغيل)
*   **ما هو؟**: هو البرنامج الذي سيقوم بتشغيل الكود البرمجي الخاص بالموقع.
*   **التحميل**: اذهب إلى [nodejs.org](https://nodejs.org/en/download) وحمل نسخة **LTS** (الأكثر استقراراً).
*   **التثبيت**: افتح الملف واضغط Next حتى النهاية (مثل أي برنامج عادي).
*   **التأكد**: افتح نافذة Terminal (Command Prompt) واكتب: `node -v`. إذا ظهر لك رقم (مثل v18.x.x) فالتثبيت ناجح.

### ب- تثبيت PostgreSQL (قاعدة البيانات)
*   **ما هي؟**: هي المكان الذي ستُحفظ فيه بيانات السيارات، الحجوزات، والمستخدمين.
*   **التحميل**: اذهب إلى [postgresql.org/download](https://www.postgresql.org/download/windows/) وحمل النسخة الخاصة بنظام ويندوز.
*   **التثبيت**:
    *   أثناء التثبيت، سيطلب منك كتابة **Password** (كلمة مرور). **احفظها جيداً** لأننا سنحتاجها لاحقاً.
    *   سيسألك عن الـ Port، اتركه كما هو (5432).
*   **الأداة الملحقة**: سيتم تثبيت برنامج معه اسمه **pgAdmin 4**، سنستخدمه لإدارة البيانات.

---

## 3. إعداد قاعدة البيانات (لأول مرة)

1.  افتح برنامج **pgAdmin 4** من قائمة ابدأ.
2.  سيطلب منك كلمة المرور التي اخترتها أثناء التثبيت.
3.  اضغط بيمين الماوس على كلمة **Databases** واختر **Create > Database**.
4.  سمِّها `car_rental_db` واضغط **Save**.
5.  اضغط بيمين الماوس على قاعدة البيانات الجديدة (`car_rental_db`) واختر **Query Tool**.
6.  افتح ملف `backend/migrations/001_initial_schema.sql` من مجلد المشروع، وانسخ كل ما بداخلة والصقه في نافذة Query Tool في pgAdmin.
7.  اضغط على زر **Execute** (أيقونة المثلث أو F5). ستظهر رسالة "Query returned successfully".

---

## 4. تجهيز ملفات الإعداد (.env)

المشروع يحتاج لمعرفة كلمة مرور قاعدة البيانات الخاصة بك.
1.  ادخل إلى مجلد `backend`.
2.  ستجد ملفاً اسمه `.env.example` (أو أنشئ ملفاً جديداً باسم `.env`).
3.  تأكد أن المحتوى كالتالي:
```env
PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=car_rental_db
DB_USER=postgres
DB_PASSWORD=ضع_هنا_كلمة_مرور_البوستقراس_الخاصة_بك
JWT_SECRET=any_random_string_123
JWT_EXPIRES_IN=7d
```

---

## 5. تشغيل المشروع (البداية الفعلية)

سنقوم الآن بتثبيت المكتبات البرمجية وتشغيل الموقع.

### الخطوة 1: تشغيل الخادم (Backend)
1. افتح مجلد `backend` في الـ Terminal.
2. اكتب الأمر التالي وانتظر حتى ينتهي:
   `npm install`
3. بعد الانتهاء، اكتب أمر التشغيل:
   `npm run dev`
   *سترى رسالة "Connected to PostgreSQL database".*

### الخطوة 2: تشغيل الواجهة (Frontend)
1. افتح نافذة Terminal جديدة (مهم جداً أن تترك الأولى تعمل).
2. انتقل لمجلد `frontend` واكتب:
   `npm install`
3. بعد الانتهاء، اكتب أمر التشغيل:
   `npm run dev`
4. سيظهر لك رابط مثل `http://localhost:3000` أو `http://localhost:5173`. اضغط عليه لفتح الموقع.

---

## 6. أدوار المستخدمين للتجربة
يمكنك تجربة النظام بثلاثة أدوار:
1.  **العميل**: حجز سيارة، رفع الهوية، الدردشة مع المورد.
2.  **المورد**: إضافة سيارات، قبول الطلبات، توثيق حالة السيارة بالصور.
3.  **الأدمن**: قبول المستخدمين والسيارات، وحل النزاعات.

---
**ملاحظة**: إذا ظهر لك خطأ "Port already in use"، فهذا يعني أن البرنامج يعمل مسبقاً في الخلفية. قم بإغلاق نوافذ الـ Terminal وأعد المحاولة.
